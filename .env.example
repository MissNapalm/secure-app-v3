# Server Configuration
PORT=8080

# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=twitterclone

# Security Configuration
JWT_SECRET=your-long-random-secret-key-min-32-chars-change-in-production

# CORS Configuration
ALLOWED_ORIGINS=http://localhost:8080

# Email Configuration (for MFA codes)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASSWORD=your-app-password
SMTP_FROM=noreply@twitterclone.app

# HTTPS Configuration (for production)
# In production, use a reverse proxy (nginx/caddy) to handle HTTPS
# Or set X-Forwarded-Proto header in your proxy to enforce HTTPS redirect
